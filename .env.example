# Optional. Only needed for cloud "save/load experiment" history.
# The core simulation and Wallpaper Mode run fully offline without these —
# leave them unset to run locally with no backend at all.
#
# Get these from your Supabase project settings (Project Settings > API):
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
